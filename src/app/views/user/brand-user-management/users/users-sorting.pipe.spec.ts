import { UsersSortingPipe } from './users-sorting.pipe';

describe('UsersSortingPipe', () => {
    it('create an instance', () => {
        const pipe = new UsersSortingPipe();
        expect(pipe).toBeTruthy();
    });
});
